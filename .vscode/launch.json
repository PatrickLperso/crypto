{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Debug avec Imports Absolus",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}